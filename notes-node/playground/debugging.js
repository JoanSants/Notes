//inspect
//:debugger;
// c = continue; n = next
//repl show the properties
//nodemon + inspect
//node --inspect-brk
//chrome://inspect/#devices

var person = {
    name:'Joan'
}

person.age = 24;

person.name = 'Jonas';

console.log(person);